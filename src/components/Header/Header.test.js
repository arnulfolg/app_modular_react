import React from 'react';
import { fireEvent, render } from '@testing-library/react';
import { act } from 'react-dom/test-utils';

describe("Header Test Suite", () => {
	
	describe('Check if elements are present', () => {
		test.todo('Check if Home link is present')
		test.todo('Check if Login link is present')
		test.todo('Check if Register link is present')
		test.todo('Check if Profile link is present')
		test.todo('Check if Close session button is present')
	})

})