import React from 'react';
import { fireEvent, render } from '@testing-library/react';
import Login from './Login';
import { act } from 'react-dom/test-utils';

describe("Login Test Suite", () => {
	
	test.todo("check if elements are present")
	test.todo("check if error elements are present")
	test.todo("check if email fails")
	test.todo("check if password fails")
	test.todo("check if wrong password shows")
	test.todo("check if wrong user shows")
})