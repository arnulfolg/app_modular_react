import React from 'react';
import { fireEvent, render } from '@testing-library/react';
import { act } from 'react-dom/test-utils';

describe("Profile Test Suite", () => {
	
	describe('Check if elements are present', () => {
		test.todo('Check if background image is present')
		test.todo('Check if profile picture is present')
		test.todo('Check if name is present')
		test.todo('Check if email is present')
		test.todo('Check if edit button is present')
	})

})